# SHARED AGENT PROTOCOL

This project uses the Beads (`bd`) CLI for state.

- **Claude:** Read this file and use `bd` commands for your loop.
- **Antigravity:** Use the `beads_manager` skill to execute these commands.
- **Cursor:** Use the terminal to run `bd ready`.
